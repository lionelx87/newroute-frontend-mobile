import { ComposeUrlPipe } from './compose-url.pipe';

describe('ComposeUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new ComposeUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
